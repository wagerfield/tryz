# Changesets

This project uses [changesets](https://github.com/changesets/changesets/tree/main) to `version` and `publish` packages.

```bash
# Create changeset
changeset

# Increment package versions and update changelogs
changeset version

# Publish packages to npm
changeset publish
```
